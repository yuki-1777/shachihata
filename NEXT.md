# 次回続きから

## 残課題（優先度順）

### 1. トリミングがまだ甘い／逆に弱い子音を削ってしまう
- 最初のキー押下時のクリック音などにも反応している可能性
- 「さ」など弱い摩擦音は s 部分が小さくてトリミングで切られてしまう（→ 子音の特徴が失われる）
- 検討：
  - キー押下〜実録音開始までに 50〜100ms のディレイを入れる（初期ノイズを録音範囲外にする）
  - もしくは、録音冒頭の固定区間（例: 最初の 50ms）を強制的に無視
  - トリミングロジックを RMS ベースから別アプローチへ：信号の連続性（自己相関）で「人声らしい区間」を抽出
  - クリック音と摩擦音はスペクトル特徴で区別可能（クリック=広帯域瞬間、摩擦音=高域持続）

### 1b. トリミング調整 UI を波形の近くに
- 現状トリミング感度スライダーは右パネル（波形拡大モーダルと離れている）
- 拡大表示モーダル内に同じスライダーを置く（or 連動表示）
- 波形を見ながら「ここまで」とリアルタイムで調整できるようにしたい

### 2. 解像度の上限
- 高解像度（1024）は印象が良いが、立体化（3D モデリング）で扱いきれない可能性
- 検討：
  - SVG 出力時にパス点数を独立指定（描画は高解像度、出力は中解像度）
  - 出力前にパスを Douglas-Peucker などで間引く
  - 「描画解像度」と「出力解像度」を分離する

### 3. オフセット方向の見直し
- 基本「両側」運用で確定の方向
- 「外向き」を残すなら、生波形の abs() ではなく **エンベロープ外向き** にすべき（振動数2倍問題が解消し、より見やすくなる）
- 検討：
  - 外向きモードを「エンベロープ外向き」に置き換え
  - または「両側 / エンベロープ外向き / 生波形外向き」の3択

### 4. 境界処理はシャープがいいかも
- フェードは過剰に滑らかすぎる印象
- デフォルトをシャープに変更を検討
- もしくはシャープ寄りの中間モード（境界の数サンプルだけ補間）

### 5. スムージングと変動量の関係
- スムージングを強くすると変動量も実質的に小さく見える
- これは「平均化で凹凸が単純化されて、結果的に最大値が下がる」現象
- 検討：
  - スムージング後に **再正規化** を入れる（現在は前段で正規化済みだが、smoothing 後に最大値を1に揃え直す）
  - もしくはスムージングの強度に応じて variation を自動補正

## 次回作業着手順
1. の対策（トリミング）を最初に → 全体の印象が安定する
2. 5. の対策（再正規化）で変動量が直感的になる
3. 3. でオフセット方向を整理
4. 2. で解像度の出力分離
5. 4. はデフォルト変更だけ
