# 聲印（Sei-in）— Voice Seal プロジェクト引き継ぎ

このドキュメントは、別のAIアシスタントに本プロジェクトの続きを依頼するための引き継ぎ書です。前提知識ゼロで作業に入れるよう、コンセプト・仕様・実装状況・未解決事項を網羅しています。

---

## 1. プロジェクト概要

### コンセプト

通常の印鑑は外径が真円ですが、本プロジェクトでは **外径が不定形の印鑑** を作ります。

- **中の文字**：その人の名前（最大4文字）
- **外径の形**：その人の **声の特徴** を反映させた一意の輪郭

「名前」と「声」という2つの個人アイデンティティ要素を組み合わせ、その人だけの印鑑を生成する Web アプリのプロトタイプです。

### 体験のフロー

1. ユーザーが自分の名前をテキスト入力（最大4文字）
2. 初期表示：真円の印鑑＋中央に名前
3. 録音開始ボタンを押し、自分の名前を発声
4. 録音中、声の特徴に応じて外径が真円から徐々に不定形へ変形（リアルタイム）
5. 録音停止 → その録音全体の平均特徴量で最終的な形が確定

---

## 2. 機能仕様（確定済み）

### 2.1 名前入力 UI

- テキスト入力欄、最大4文字
- 文字数による配置ルール：
  - **1文字**：中央
  - **2文字**：縦に中央（上下に並べる）
  - **3文字**：縦3列（暫定仕様、要確認）
  - **4文字**：2×2配置（伝統的な印章の読み順：右上 → 左上 → 右下 → 左下）
- 書体：明朝体（`Shippori Mincho` 推奨）

### 2.2 録音・解析

- Web Audio API（`getUserMedia` + `AnalyserNode`）でマイク入力をリアルタイム解析
- ノイズゲート：RMS が一定値（例 0.02）以下のフレームは無視
- 録音中はリアルタイムに形を変形させる（プレビュー）
- 録音停止時、録音中の全有効フレームの特徴量を **平均** して最終形を確定

### 2.3 特徴量モード（3つを切り替え可能）

ユーザーが切り替えられる。各モードの説明文をアプリ内に表示する。

#### B — スペクトル（倍音構成）
- 円周に低音→高音を配置（対数スケール推奨）
- 各周波数の強度を半径のオフセットにマッピング
- 声質の豊かさ＝輪郭の複雑さ。声紋的、細かい凹凸が出る

#### C — フォルマント（声道の共鳴）
- スペクトルから2〜3個の共鳴ピーク（F1〜F3、200〜3500Hz帯）を検出
- 各フォルマントを円周上の角度に対応させ、大きな膨らみ（ローブ）として描画
- 落ち着いた数個の凹凸の有機的な形になる。発音する母音で形が変わる

#### D — 複合
- **基本周波数 F0**（声の高さ）→ 全体サイズ
- **スペクトル重心**（声の明るさ）→ 形の偏り・非対称
- **倍音の分散**（声の粗さ）→ 輪郭の細かい揺らぎ
- 複数の声特徴を同時に翻訳する総合モード

> 当初候補にあった「A：波形をそのまま円周にマッピング」は、発声の瞬間ごとに形が変わりすぎて固有性が出にくいため **採用しない**。

### 2.4 描画

- キャンバスは画面いっぱい
- 真円から徐々に変形させる（毎フレーム lerp で目標形状に追従、滑らかに）
- 円周上のサンプリング点数：256点程度
- 円形の閉じた滑らかな曲線（quadratic curve や Catmull-Rom など）で点を結ぶ
- 各点の半径を周方向に軽くスムージング（隣接点の平均化を数回適用）してギザつきを抑える

### 2.5 ビジュアルデザイン（確定）

- **配色**：朱色（印鑑赤、`#c8331c` 系）と和紙色（`#f4ede0` 系）
- **書体**：明朝体（`Shippori Mincho`）
- **テクスチャ**：和紙の粒子、印影のかすれ、軽い radial gradient で印影の深みを出す
- **トーン**：和モダン、墨と朱の世界観

### 2.6 UI 構成

3カラムレイアウト：

- 左：タイトル「聲印」、コンセプト説明、名前入力欄
- 中央：印鑑のキャンバス（変形する印影）、録音インジケータ
- 右：特徴量モードのタブ（B/C/D）、モード説明文、録音開始/停止ボタン、リセット（真円に戻す）ボタン、ステータス表示

---

## 3. 実装状況

単一の HTML ファイルでプロトタイプを作成済み（バニラ JS、外部ライブラリなし、Google Fonts のみ）。以下のリポジトリ的構造ではなく、1ファイルで完結する設計。

実装済みの主な処理：

- 名前入力 UI と文字数別配置（1/2/3/4 文字）
- マイク取得、`AnalyserNode` で FFT・時間波形の取得
- 3モード（B/C/D）の特徴量計算ロジック
- 録音中フレームの蓄積、停止時の平均化による形状確定
- lerp による滑らかな形状追従アニメーション
- 朱色・和紙テクスチャ・明朝体のビジュアルデザイン
- モード切替時の説明文の差し替え

---

## 4. 既知の問題・未解決事項

### 4.1 マイクアクセスの問題（最優先）

**現状**：`file://` プロトコルでローカルHTMLを直接開くと、ブラウザのセキュリティ制約で `getUserMedia` が拒否される。

**必要な対応**：
- `https://` または `localhost` 経由で配信する必要がある
- 解決策の候補：
  - VSCode の Live Server 拡張で配信
  - `python3 -m http.server` などのローカルサーバ
  - Vercel / Netlify / GitHub Pages 等にデプロイ
  - もしくは Claude Artifact / CodeSandbox 等のオンライン実行環境
- **次のAIへの依頼**：このプロトタイプを **オンラインでマイク許可が出る形でデプロイ可能な構成** に整えてほしい。あるいは、デプロイ手順を込みで案内してほしい。

### 4.2 形の変動量の調整

現状は控えめ（半径 ±15〜25%）。印鑑として「落ち着き」と「個人差の見えやすさ」のバランス調整が必要。実際に複数人で試して調整したい。

### 4.3 再現性

同じ人が同じ名前を言ったとき、毎回似た形になるかが「その人の印鑑」としての理想。現状は1回の録音内の平均で確定するが、発話タイミングで揺れる。改善案：

- 録音時間を一定以上必要とする（例：2秒以上）
- 複数回録音して平均を取る
- 録音中の安定区間（ピッチが安定している区間）のみを採用する

### 4.4 モード切替時の挙動

現状、モードを切り替えても録音し直さないと反映されない（録音時の特徴量から最終形を保存しているため）。改善案：

- 録音中の全フレームの **生データ**（FFTの履歴）を保存しておき、停止後にモード切替で再計算できるようにする

### 4.5 3文字配置

仕様で指定がなかったため、暫定で「縦3列」とした。要確認。

### 4.6 モード C（フォルマント）の母音差

「あいうえお」で実際にどれくらい形が変わるか未検証。差が小さければ、フォルマント検出の強調や角度マッピングを調整する余地あり。

---

## 5. 次の AI への依頼事項

このコンテキストを引き継いだAIには、以下を期待します：

1. **マイクアクセスが動作する環境**でプロトタイプを動かせるように整えてほしい（デプロイ or 実行環境込みで）
2. 実際に動かしてみて、上記「4. 既知の問題・未解決事項」を順に潰してほしい
3. 特に **B / C / D モードの違いが触ってわかるか** を検証し、必要なら特徴量の強調を調整してほしい
4. デザイン面でも、印鑑として成立しているか（朱色のかすれ、明朝の刻まれ感、不定形でも印鑑に見えるか）の品質向上を歓迎

---

## 6. 技術スタック・ファイル構成

- 単一HTMLファイル（`voice_seal.html`）
- バニラ JavaScript、外部ライブラリなし
- Google Fonts: `Shippori Mincho`, `Klee One`
- Web Audio API（`AudioContext`, `AnalyserNode`, `getUserMedia`）
- Canvas 2D API で描画
- レスポンシブ対応（1100px以下で縦積みレイアウト）

実装ファイルはこのドキュメントと一緒に渡される `voice_seal.html` を参照してください。

---

## 7. 補足：判断の経緯

- 当初は特徴量A（波形そのまま）も候補にあったが、「固有性が出にくい」という理由でユーザーが除外を決定
- 形の確定方法は「録音中の最後の瞬間の形を固定」と「録音全体の平均」の2案があり、後者を採用（再現性・固有性のため）
- 印鑑色は朱色（伝統色）を採用
- 書体は篆書体ではなく明朝体を採用（実装容易性とモダンな見え方のため）

---

以上です。